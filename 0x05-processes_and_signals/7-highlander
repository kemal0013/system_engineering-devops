#!/usr/bin/env bash
# Script that runs indefinitely and handles SIGTERM signals gracefully

# Function to handle SIGTERM
handle_term() {
    echo I am invincible!!!
}

# Trap SIGTERM signal before starting the loop
trap 'handle_term' SIGTERM

# Main infinite loop
while true; do
    echo "To infinity and beyond"
    sleep 2
done
